import React from "react";

const Header = (props) => {
    return (
        <div>
        <h1 className="p-4 font-bold text-black text-3xl">{props.title}</h1>
    </div>

    )



}
export default Header;